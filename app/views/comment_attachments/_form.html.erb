<%= form_for(@comment_attachment) do |f| %>
  <% if @comment_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment_attachment.errors.count, "error") %> prohibited this comment_attachment from being saved:</h2>

      <ul>
      <% @comment_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.number_field :comment_id %>
  </div>
  <div class="field">
    <%= f.label :document %><br>
    <%= f.text_field :document %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
