SelectValue = $('select[name="select"]').val();
num = SelectValue - 1;
a = [];
<% @template.each do | t |%>
a.push("<%= escape_javascript(t.body) %>");
<% end %>

$("#order_order_comment").val(a[num]);